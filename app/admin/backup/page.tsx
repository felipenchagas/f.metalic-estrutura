"use client"
import { useState } from "react"
export default function BackupPage() { const [loading, setLoading] = useState(false); const [message, setMessage] = useState(""); const create = async () => { setLoading(true); setMessage(""); try { const response = await fetch("/api/admin/backup", { method: "POST" }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Não foi possível gerar o backup."); setMessage("Backup salvo no servidor. O download começou."); window.location.assign(`/api/admin/backup?file=${encodeURIComponent(data.name)}`); } catch (error) { setMessage(error instanceof Error ? error.message : "Não foi possível gerar o backup."); } finally { setLoading(false); } }; return <div className="max-w-2xl text-white"><p className="text-[11px] uppercase tracking-[.2em] text-[#C0392B]">Segurança</p><h1 className="mt-2 text-3xl font-black">Backup dos dados</h1><p className="mt-3 text-sm leading-relaxed text-[#999]">Gera uma cópia completa dos dados administrativos, preserva-a no servidor e baixa o mesmo arquivo.</p><button onClick={create} disabled={loading} className="mt-6 rounded-xl bg-[#C0392B] px-5 py-3 text-sm font-bold text-white disabled:opacity-60">{loading ? "Gerando backup..." : "Gerar e baixar backup"}</button>{message && <p className="mt-4 text-sm font-semibold text-[#ccc]">{message}</p>}</div> }
